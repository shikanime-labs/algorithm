# Contributing to algorithm

My algorithms sketchbook

## Workflow

Fork, branch off `main`, open a PR against `main`. One logical change per PR.

## Environment

```sh
nix develop
```

## Validation

Solutions must compile/run; include sample input/output.

Security issues: see [SECURITY.md](SECURITY.md).
