# shellcheck shell=bash

# Intentionally empty: the root .envrc manages the devShell via `use flake .`.
# A nested .envrc with `use flake ..#algorithm-javascript` is orphaned and breaks `devenv test`.
