# shellcheck shell=bash

# Intentionally empty: the root .envrc manages the devShell via `use flake .`.
# A nested `use flake ..#algorithm-ocaml` here breaks `devenv test` because
# enterTest (dune runtest) enters this directory and devenv can no longer
# resolve the flake from the subdir, failing with "not part of a flake".
